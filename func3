//Program to find the sum of n no.s using function

#include<stdio.h>
int sum()
{
int i=1,sm=0,n;
printf("Enter range to find sum: ");
scanf("%d",&n);
while(i<=n)
{
sm+=i;
i++;
}
printf("\nThe required sum is: %d",sm);
}
int main()
{
sum();
return 0;
}
