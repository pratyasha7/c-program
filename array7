//Sum of the elements of an array
#include<stdio.h>
int main()
{
int a[10],i,sum=0;
for(i=0;i<10;i++)
{
printf("Enter number to add: ");
scanf("%d",&a[i]);
}
for(i=0;i<10;i++)
    sum+=a[i];
printf("Sum of array elements: %d",sum);
return 0;
}
