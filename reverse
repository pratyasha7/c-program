//Print the reverse of a number
#include<stdio.h>
int main()
{
int no,rev=0;
printf("Enter a number to find reverse: ");
scanf("%d",&no);
while(no>=1)
{
rev= (rev*10)+(no%10);
no/=10;
}
printf("\nReversed form: %d",rev);
return 0;
}
