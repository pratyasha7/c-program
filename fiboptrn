//To print pattern using '*' whose logic would be that of fibonacci number's.
#include<stdio.h>
int main()
{
int i=1,n,j,a=0,b=1,sum=0;
printf("Enter no of rows: ");
scanf("%d",&n);
while(i<=n)
{
printf("\n");
a=b;
b=sum;
sum= a+b;
j=1;
while(j<=sum)
{
printf("*");
j++;
}
i++;
}
return 0;
}
