#include<stdio.h>
int main()
{
int rev=0,no,n;
printf("Enter a number: ");
scanf("%d",&no);
n=no;
while(no>=1)
{
rev=(rev*10)+(no%10);
no=(no/10);
}
if(rev==n)
printf("It is a palindrome number",no);
else
printf("It's not a palindrome number",no);
return 0;
}
